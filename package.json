{
  "name": "helpmebrands-reward",
  "version": "0.1.0",
  "private": true,
  "description": "HelpMe Reward monorepo: the Flutter app, the service tier, the static site and the Pulumi infrastructure. The npm workspaces are the two Pulumi programs.",
  "workspaces": [
    "infra",
    "infra-repo"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "allowScripts": {
    "protobufjs@7.6.6": true,
    "fsevents@2.3.3": true
  }
}
