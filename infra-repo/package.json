{
  "name": "reward-app-infra-repo",
  "version": "0.1.0",
  "private": true,
  "description": "Pulumi program for the repository-wide GitHub configuration of HelpMe Reward: the develop ruleset.",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pulumi/github": "^6.15.0",
    "@pulumi/pulumi": "^3.0.0",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  }
}
